description: Extracts and saves a volume signature catalog file from a given shielded
  VM template disk
synopses:
- Save-VolumeSignatureCatalog [-TemplateDiskPath] <String> [-VolumeSignatureCatalogPath]
  <String>
options:
  -TemplateDiskPath String:
    required: true
  -VolumeSignatureCatalogPath String:
    required: true
